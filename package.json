{
  "name": "chartex-airtable-sync",
  "version": "1.0.0",
  "description": "Sync Chartex music data (song usage, rising sounds, creators) into the Florra CRM Airtable base for Ebril.",
  "main": "src/sync.js",
  "scripts": {
    "sync": "node src/sync.js",
    "sync:dry": "node src/sync.js --dry-run",
    "sync:demo": "node src/sync.js --demo"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
